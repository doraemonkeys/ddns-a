// Entry point is a placeholder until async runtime integration is planned.
// Excluded from coverage as it contains no testable logic.
#[cfg(not(tarpaulin_include))]
fn main() {
    println!("Hello, world!");
}
